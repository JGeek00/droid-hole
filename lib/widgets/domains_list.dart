import 'package:flutter/material.dart';

class DomainsList extends StatelessWidget {
  const DomainsList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}